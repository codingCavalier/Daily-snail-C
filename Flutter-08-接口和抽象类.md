### 接口（行为契约）
- 没有 interface 关键字用于定义接口
- **任何类都隐式定义了一个接口**
- 没有构造函数
- 使用 implements 实现一个接口
- 如果实现接口，就要全部实现里面的成员方法和成员变量

### 抽象类（代码共享）
- 定义方式：abstract class A
- 有构造函数
- 使用 extends 继承一个抽象类
- 如果本身不是抽象类，也不是接口（即没用implements实现接口），则必须实现抽象类里的抽象方法
